name: ai1-satellite-tools
version: 1
description: >
  Satellite toolkit — installs and manages Ai1 Packages; registers and syncs
  with the Ai1 Platform Hub.
installer: 1
after: scripts/install-satellite-tools.sh
flags:
  - name: --skip-extra
    description: Skip symlink and cron setup (forwarded to hook scripts).
components:
  skills:
    - path: skills/ai1-satellite-tools
      version: 1
      install_type: org
