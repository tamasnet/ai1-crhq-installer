{
  "name": "ai1-satellite-tools",
  "version": "1.0.0",
  "description": "Manage a CRHQ satellite's resources: install versioned packages (skills, recipes, agents, jobs, services), sync/mirror live components back into a package (backup), and run the satellite's Ai1 Platform Hub client (register, config, heartbeat, packages)",
  "private": true,
  "bin": {
    "ai1-install": "./scripts/install.mjs",
    "ai1-sync": "./scripts/sync.mjs",
    "ai1-remote": "./scripts/remote.mjs"
  },
  "scripts": {
    "install-bundle": "node scripts/install.mjs",
    "test": "node tests/skill-recipe.test.mjs && node tests/agent.test.mjs && node tests/job.test.mjs && node tests/runner.test.mjs && node tests/service.test.mjs && node tests/filter.test.mjs && node tests/identity.test.mjs && node tests/options.test.mjs && node tests/install-log.test.mjs && node tests/sync.test.mjs && node tests/remote.test.mjs"
  },
  "engines": {
    "node": ">=18"
  },
  "comment": "No runtime dependencies \u2014 yaml is vendored at scripts/lib/vendor/yaml.mjs so the skill installs without npm install. To regenerate that bundle, install yaml as a devDependency and run the esbuild command in its header.",
  "dependencies": {}
}
